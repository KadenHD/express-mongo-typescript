import Encrypter from '../library/Encrypter';
import Logging from '../library/Logging';

describe('src/library/Encrypter.ts', () => {
    describe('encrypt', () => {
        it('', () => {});
    });

    describe('decrypt', () => {
        it('', () => {});
    });
});

describe('src/library/Logging.ts', () => {
    describe('', () => {
        it('', () => {});
    });

    describe('', () => {
        it('', () => {});
    });
});
